# Base URL for the UI test projects (chromium/firefox/webkit/mobile)
BASE_URL=https://demowebshop.tricentis.com

# Base URL for the API test project
API_BASE_URL=https://fakestoreapi.com

# REQUIRED for tests/ui/login.spec.ts (positive login/logout tests) and
# tests/ui/checkout.spec.ts (via the authenticatedPage fixture in
# fixtures/auth.fixture.ts). Must be a real, already-registered
# Demo Web Shop account. Never commit real values here or paste them
# into chat — .env is gitignored; only .env.example is committed.
EXISTING_USER_EMAIL="shelkerahul@gamil.com"
EXISTING_USER_PASSWORD="Snehal@1498"
